(program
 (def
  (a)
  (suite
   (class (func)
     (suite
      (= (a) 2)
      (def (__init__ self) (suite (= ((indexed self (dot b))) a)))))
   (class (funnyfunc func) (suite (= (c) "whi")))
   (return funnyfunc)))
 (expr (indexed print (called (indexed a (called) (called) (dot c))))))

