# tests line join indentation
'start'
    \
          \
       \
                     \
         
               "If empty lines end line join series, then this should not be followed by an indent"
               "If empty lines end line join series, then this should not be preceded by a indent"
